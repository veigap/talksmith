{% import "_macros.j2" as m %}
{% call m.stage(section, title) %}
<div class="ci"><div class="citext">
{% for b in leads %}<p class="lead">{{ b }}</p>{% endfor %}
</div>{{ embed(images[0][0], images[0][1]) if images else embed("", None) }}</div>
{% endcall %}
