<div class="stage cover"><div class="hero">{% if s.section %}<p class="steyebrow">{{ s.section }}</p>{% endif %}<span class="qa">{{ s.title }}</span>{% if s.body %}<span class="qc">{{ s.body }}</span>{% endif %}</div></div>
