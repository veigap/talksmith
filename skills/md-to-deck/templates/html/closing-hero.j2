<div class="stage cover"><div class="hero">{% if section %}<p class="steyebrow">{{ section }}</p>{% endif %}<span class="qa">{{ title }}</span>{% if body %}<span class="qc">{{ body[0] }}</span>{% endif %}</div></div>
