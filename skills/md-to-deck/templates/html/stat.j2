{% import "_macros.j2" as m %}
{% call m.stage(section, title) %}
<div class="stats" style="grid-template-columns:repeat({{ cols }},1fr)">
{% for it in items %}<div class="stat"><span class="statn">{{ it.label }}</span><span class="statl">{{ it.body }}</span></div>{% endfor %}
</div>
{% endcall %}
